import { createColour } from "../helpers/createColour";


const WHITE = createColour(255, 255, 255);
const BLUE = createColour(0, 0, 255);
const GREEN = createColour(0, 255, 0)
const RED = createColour(255, 0, 0);

export const colours = { White: WHITE, BLUE: BLUE, GREEN: GREEN, RED: RED }